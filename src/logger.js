import { Collection } from "collect.js";
import collect from "collect.js";

const collectioin = new Collection([10, 20, 30]);
const count = collectioin.count();
console.log("count:" + count);

console.log("Message");

export default collect([1, 2, 3]).all();
